import classes from './nullchat.module.css';

const Chat = (props) => {
    return (
        <div className={classes.nullMessanger}>
            <h3>Select dialog to chat</h3>
        </div>);
}
export default Chat;